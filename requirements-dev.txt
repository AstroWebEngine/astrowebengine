# Development and test dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0          # FastAPI TestClient transport
